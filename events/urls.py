from django.urls import path
from .views import *

urlpatterns = [

    # path('home/', home),
    # path('', Events, name="event_list"),
    # path('students/event_reg/', EventRegistration, name="event_reg")
]
